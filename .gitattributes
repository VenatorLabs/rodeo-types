# https://help.github.com/articles/dealing-with-line-endings/
# -----------------------------------------------------------
* text=auto eol=lf
# These are explicitly windows files and should use crlf
*.bat                     text eol=crlf
# Binary files that should be tracked only as 1 single blob (git-lfs)
**/gradle-wrapper.jar filter=lfs diff=lfs merge=lfs -text
